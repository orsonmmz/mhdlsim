#include "elaborator.h"
#include "simulator.h"

int main(int argc, char*argv[])
{
    /*
     register simulators (i.e. ghdl & systemverilog)

     for each input file {
        create a simulator instance that works with associated file
     }

     manager.run();
     */

    return 0;
}
