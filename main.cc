#include "elaborator.h"
#include "simulator.h"

int main(int argc, char*argv[])
{
    /*

     create argument parser instance.
     forward input to the argument parser.
     
     create manager instance.

     // for verilog files
     if( *argument parser has verilog files as input* ) {
        create verilog sim.
        pass files to verilog sim.
        add instance to the manager.
     }
     // for VHDL files, the dual of the above pseudocode.

     manager.run();
     */

    return 0;
}
